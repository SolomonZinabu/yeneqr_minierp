# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — NEVER track production env (contains server-specific URLs + secrets)
# .env.development is OK to track (localhost only)
.env
.env.local
.env.*.local
.env.production
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log

# Skills directory (internal)
skills/

# Local DB
db/
*.db
*.db-journal

# Download dir (generated output)
download/

# Agent context (internal)
agent-ctx/

# Worklog (internal)
worklog.md

# Examples
examples/

# Internal scripts
.zscripts/

# Mini-services (deployed separately)
mini-services/

# Themall colors cache
themall_colors.json

# Tool result artifacts (internal agent cache, never track)
tool-results/
