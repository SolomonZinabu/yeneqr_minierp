# Development environment — used when NODE_ENV=development
# Base URL for QR codes, payment callbacks, and link generation
NEXT_PUBLIC_BASE_URL=http://localhost:3000
# Alias used by forgot-password route
NEXT_PUBLIC_APP_URL=http://localhost:3000
